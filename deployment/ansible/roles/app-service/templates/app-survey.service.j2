[Unit]
Description=Intake24 Survey client
After=network.target

[Service]
Type=simple
User={{ app.user }}
Group={{ app.group }}
Restart=on-failure
WorkingDirectory={{ app.directory }}/apps/survey
ExecStart=/usr/bin/node server/index.js

[Install]
WantedBy=multi-user.target
