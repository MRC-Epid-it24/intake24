[Unit]
Description=Intake24 Survey client
After=network.target

[Service]
Type=simple
User={{ env.user }}
Group={{ env.group }}
Restart=on-failure
WorkingDirectory={{ env.directory }}/apps/survey
ExecStart=/usr/bin/node server/index.js

[Install]
WantedBy=multi-user.target
