[Unit]
Description=Intake24 Admin client
After=network.target

[Service]
Type=simple
User={{ env.user }}
Group={{ env.group }}
Restart=on-failure
WorkingDirectory={{ env.directory }}/apps/admin
ExecStart=/usr/bin/node server/index.js

[Install]
WantedBy=multi-user.target
