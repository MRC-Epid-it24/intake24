import{_ as e,c as a,o as t,a as i}from"./app.a95cc4c4.js";const g='{"title":"Get started","description":"","frontmatter":{},"headers":[{"level":2,"title":"Repository","slug":"repository"},{"level":2,"title":"API Server","slug":"api-server"},{"level":2,"title":"Database","slug":"database"},{"level":2,"title":"Admin client","slug":"admin-client"},{"level":2,"title":"Survey client","slug":"survey-client"},{"level":2,"title":"Documentation","slug":"documentation"}],"relativePath":"overview/get-started.md","lastUpdated":1642613177059}',o={},r=i(`<h1 id="get-started" tabindex="-1">Get started <a class="header-anchor" href="#get-started" aria-hidden="true">#</a></h1><p>This section briefly describes how to configure, build and start the Intake24 components.</p><h2 id="repository" tabindex="-1">Repository <a class="header-anchor" href="#repository" aria-hidden="true">#</a></h2><p>Clone the repository</p><div class="language-sh"><pre><code>git clone https://github.com/MRC-Epid-it24/intake24
</code></pre></div><p>Repository is set as mono-repository with <a href="https://docs.npmjs.com/cli/v7/using-npm/workspaces" target="_blank" rel="noopener noreferrer">workspaces</a>.</p><p>Shared components are in <code>packages</code> folder. Applications are in <code>apps</code> folder.</p><p>Install <code>pnpm</code> using built-in <code>npm</code> or see <a href="https://pnpm.io" target="_blank" rel="noopener noreferrer">pnpm docs</a> for further options.</p><div class="language-sh"><pre><code>npm install -g pnpm
</code></pre></div><p>You can then install all dependencies from root-level running</p><div class="language-sh"><pre><code>pnpm install
</code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Code is using <code>.env </code> environment files. Each project contains a template file (<code>.env-template</code>). If you&#39;re setting up fresh project, you can run CLI command, which generates all the files and couple of required specific keys. Follow the <a href="/overview/cli/">CLI instructions</a> and how to run <a href="/overview/cli/generate-env.html">generate-env</a>.</p></div><h2 id="api-server" tabindex="-1">API Server <a class="header-anchor" href="#api-server" aria-hidden="true">#</a></h2><p>Navigate to <code>apps/api</code> directory.</p><div class="language-sh"><pre><code>cd apps/api
</code></pre></div><p>Copy template of configuration file and edit it as required. Please see <a href="/config/">Configuration</a> section for detailed description of configuration options.</p><div class="language-sh"><pre><code>cp .env-template .env
</code></pre></div><p>Build the application</p><div class="language-"><pre><code>pnpm build
</code></pre></div><p>Start the application</p><div class="language-"><pre><code>pnpm start
</code></pre></div><div class="tip custom-block"><p class="custom-block-title">For production, consider</p><ol><li>Deploying as dedicated service. Refer to your OS environment how to set that up.</li><li>User process manager, e.g. <a href="https://pm2.keymetrics.io" target="_blank" rel="noopener noreferrer">PM2</a></li><li>Deploy behind proper http server, e.g. <a href="https://www.nginx.com" target="_blank" rel="noopener noreferrer">Nginx</a>, <a href="https://www.apache.org" target="_blank" rel="noopener noreferrer">Apache</a> etc.</li></ol></div><p>Deployment section / ansible scripts provide examples how to use dedicated service on Ubuntu and run it behind Nginx reverse proxy.</p><h2 id="database" tabindex="-1">Database <a class="header-anchor" href="#database" aria-hidden="true">#</a></h2><ul><li>project is using <a href="https://github.com/sequelize/cli" target="_blank" rel="noopener noreferrer">sequelize-cli</a></li><li>database layer is located in <code>packages/db</code></li><li>config / migration files (per database) are store in respective <code>packages/db/sequelize/{database}</code> folders</li></ul><p>Set up <code>.env</code> file if running independently of API server so it can load connection details</p><div class="language-sh"><pre><code>cp .env-template .env
</code></pre></div><p>Migrations can be executed from root-level or package level with following commands</p><div class="language-sh"><pre><code>pnpm db:migrate:foods

pnpm db:migrate:system
</code></pre></div><h2 id="admin-client" tabindex="-1">Admin client <a class="header-anchor" href="#admin-client" aria-hidden="true">#</a></h2><p>Navigate to <code>apps/admin</code> directory.</p><div class="language-sh"><pre><code>cd apps/admin
</code></pre></div><p>Copy template of configuration file and edit it as required. Please see <a href="/config/">Configuration</a> section for detailed description of configuration options.</p><div class="language-sh"><pre><code>cp .env-template .env
</code></pre></div><p>Build the application</p><div class="language-"><pre><code>pnpm build
</code></pre></div><p>Start the application</p><div class="language-"><pre><code>pnpm start
</code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>This should only be used if you host the client on separate domain. It will start separate http server to serve the application.</p></div><p>Serve the application locally in development mode with hot-reload</p><div class="language-"><pre><code>pnpm dev
</code></pre></div><h2 id="survey-client" tabindex="-1">Survey client <a class="header-anchor" href="#survey-client" aria-hidden="true">#</a></h2><p>Navigate to <code>apps/survey</code> directory.</p><div class="language-sh"><pre><code>cd apps/survey
</code></pre></div><p>Copy template of configuration file and edit it as required. Please see <a href="/config/">Configuration</a> section for detailed description of configuration options.</p><div class="language-sh"><pre><code>cp .env-template .env
</code></pre></div><p>Build the application</p><div class="language-"><pre><code>pnpm build
</code></pre></div><p>Start the application</p><div class="language-"><pre><code>pnpm start
</code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>This should only be used if you host the client on separate domain. It will start separate http server to serve the application.</p></div><p>Serve the application locally in development mode with hot-reload</p><div class="language-"><pre><code>pnpm dev
</code></pre></div><h2 id="documentation" tabindex="-1">Documentation <a class="header-anchor" href="#documentation" aria-hidden="true">#</a></h2><p>Documentation is built with <a href="https://vitepress.vuejs.org" target="_blank" rel="noopener noreferrer">vitepress</a>.</p><p>Serve the application locally in development mode with hot-reload</p><div class="language-"><pre><code>pnpm docs:dev
</code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Documentation is automatically rebuilt and deployed to dedicated site whenever source code is updated.</p></div>`,58),n=[r];function p(s,d,c,l,h,u){return t(),a("div",null,n)}var m=e(o,[["render",p]]);export{g as __pageData,m as default};
