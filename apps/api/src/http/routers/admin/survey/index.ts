import { respondent } from './respondent.router';
import { respondentCustomField } from './respondent-custom-field.router';
import { session } from './session.router';
import { submission } from './submission.router';

export default { respondent, respondentCustomField, session, submission };
