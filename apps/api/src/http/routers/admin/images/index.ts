import { asServedImage } from './as-served-image.router';
import { asServedSet } from './as-served-set.router';
import { drinkwareSet } from './drinkware-set.router';
import { guideImage } from './guide-image.router';
import { imageMap } from './image-map.router';

export default {
  asServedImage,
  asServedSet,
  drinkwareSet,
  guideImage,
  imageMap,
};
