import { job } from './job.contract';
import mfa from './mfa';
import { personalAccessToken } from './personal-access-tokens.contract';
import { profile } from './profile.contract';

export default { job, mfa, personalAccessToken, profile };
