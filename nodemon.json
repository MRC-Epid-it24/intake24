{
  "restartable": "rs",
  "verbose": true,
  "exec": "node --inspect=5858 --require ts-node/register --require tsconfig-paths/register src/index.ts",
  "watch": ["src"],
  "env": {
    "NODE_ENV": "development"
  },
  "ext": "ts,js,json"
}
